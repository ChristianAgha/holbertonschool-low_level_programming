#include "holberton.h"
/**
 * add - adds two integer and returns the result
 * @a: first number
 * @b: second number
 * r: addition result
 * Return: result
 */
int add(int a, int b)
{
	int r = a + b;

	return (r);
}
