#include "holberton.h"

/**
 * add - adds two integer and returns the result
 * @a: first number
 * @b: second number
 * Return: result
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
